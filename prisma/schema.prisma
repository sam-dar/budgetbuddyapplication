generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")    // Direct (5432) → migrate/introspect

}
model Expense {
  id        String   @id @default(uuid())
  title     String
  amount    Float
  category  String
  date      DateTime
  userId    String   @db.Uuid
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
